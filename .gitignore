node_modules/
dist/
dist-server/
.vite/
.npm-cache/
.tmp/
coverage/
playwright-report/
test-results/
vite*.log
*.local
# Delivery-loop observability (structured run log; per-run, never commit).
.loop-log.jsonl
# Live integration core keys (LLM/embedding); per-machine, never commit.
.env.core
.agent-sessions/
.claude/*
# Track shared project skills + agents, but keep local settings/locks out.
!.claude/skills/
!.claude/agents/
# Runtime branding override (per-deployment; copy from public/config.example.json)
/public/config.json
# impeccable design-skill detector cache (transient; the design system itself
# lives in DESIGN.md + .impeccable/design.json, which ARE tracked).
.impeccable/hook.cache.json

# impeccable critique snapshots (local evaluation history, per-run)
.impeccable/critique/
