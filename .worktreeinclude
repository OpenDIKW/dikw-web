# Files copied into new worktrees (.gitignore syntax).
# Only paths that match AND are gitignored are copied — tracked files
# (.env.example, .env.core.example) are never duplicated.
.env.local
.env.core
